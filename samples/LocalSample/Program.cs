using Nj.EntityFrameworkCore.LibSql;

Console.WriteLine(LibSqlProviderInfo.GetScaffoldStatus());
Console.WriteLine("LocalSample: UseLibSql + file database arrives with provider core (WP-04).");
return 0;
